[DEFAULT]
number_of_images = 100  # total for all websites
max_workers = 6  # upper limit for threads. Is it needed when using asyncio?
request_timeout = 2000  # ms
to_force_refetch = false  # instructs the script whether to fetch all images straight away or first check if each image was already downloaded and could be taken from FS instead
input_filename = input.txt
originals_dirname = img-originals
output_dirname = img-rotated
log_dirname = log
